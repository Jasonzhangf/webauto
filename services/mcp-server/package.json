{
  "name": "@webauto/mcp-server",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "webauto-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "esbuild index.ts --bundle --platform=node --format=esm --outfile=dist/index.js --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\" --external:@modelcontextprotocol/sdk",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.0.0",
    "@types/node": "^24.0.0"
  }
}
