{
  "name": "@webauto/camoufox-cli",
  "version": "1.0.0",
  "description": "Camoufox Browser CLI - Standalone browser automation tool",
  "type": "module",
  "bin": {
    "camo": "./bin/camoufox.mjs"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "install:global": "npm run build && npm install -g .",
    "uninstall:global": "npm uninstall -g @webauto/camoufox-cli"
  },
  "keywords": [
    "browser",
    "automation",
    "camoufox",
    "cli"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {},
  "peerDependencies": {
    "camoufox": "^0.1.0"
  }
}