{
  "anthropic_to_openai": {
    "requestTemplate": {
      "model": "{{model}}",
      "messages": "{{messages}}",
      "temperature": "{{temperature}}",
      "max_tokens": "{{max_tokens}}",
      "stream": "{{stream}}",
      "tools": "{{tools}}",
      "tool_choice": "{{tool_choice}}"
    },
    "responseTemplate": {
      "id": "{{id}}",
      "object": "{{object}}",
      "created": "{{created}}",
      "model": "{{model}}",
      "choices": "{{choices}}",
      "usage": "{{usage}}"
    }
  }
}