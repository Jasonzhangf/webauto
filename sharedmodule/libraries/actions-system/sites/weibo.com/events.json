{
  "version": "1.0.0",
  "events": [
    {
      "key": "appear",
      "label": "元素出现(appear)",
      "node": "EventDrivenOptionalClickNode",
      "params": { "click": false, "highlight": true, "maxWaitMs": 8000, "pollIntervalMs": 300 }
    },
    {
      "key": "action:click",
      "label": "点击(action:click)",
      "node": "EventDrivenOptionalClickNode",
      "params": { "click": true, "highlight": true, "maxWaitMs": 8000, "pollIntervalMs": 300 }
    },
    {
      "key": "scroll:intoview",
      "label": "滚动到可视",
      "node": "JavaScriptExecutionNode",
      "params": {
        "script": "return (function(){ const el=document.querySelector(window.__webautoTmpSelector||''); if(!el) return {ok:false}; el.scrollIntoView({behavior:'instant',block:'center'}); return {ok:true}; })();"
      }
    }
  ]
}

