{
  "name": "分析1688搜索框",
  "description": "分析1688主页的搜索框结构",
  "version": "1.0.0",
  "nodes": [
    {
      "id": "start",
      "type": "StartNode",
      "next": ["analyze_search"]
    },
    {
      "id": "analyze_search",
      "type": "JavaScriptExecutionNode",
      "config": {
        "script": "const inputs = document.querySelectorAll('input[type=\"text\"], input[placeholder*=\"搜索\"], input[placeholder*=\"找\"], .search-input input, #search-input'); console.log('找到的输入框数量:', inputs.length); inputs.forEach((input, index) => { console.log(`输入框${index + 1}:`, { id: input.id, className: input.className, placeholder: input.placeholder, name: input.name, type: input.type }); }); return { count: inputs.length, inputs: Array.from(inputs).map(input => ({ id: input.id, className: input.className, placeholder: input.placeholder, name: input.name, type: input.type })) };"
      },
      "next": ["end"]
    },
    {
      "id": "end",
      "type": "EndNode",
      "config": {
        "cleanupHighlights": false,
        "persistSession": true
      }
    }
  ]
}